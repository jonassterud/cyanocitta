use nostr::prelude::*;

/*
pub fn _example_code() {
    let client = Client::new();;
    client.add_relay("wss://relay");
    client.connect_relay("wss://relay");

    let broadcast = true;
    if broadcast {
        client.broadcast_message(ClientMessage::new_req(...));
    } else {
        client.send_message(ClientMessage::new_req(...));
    }

    let listener = client.create_listener(...);
    tokio::spawn(async move {
        if let Ok(relay_message) = listener.recv().await {
            // ...
        }
    });
}
*/
